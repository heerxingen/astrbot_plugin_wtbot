{
  "weblate_url": {
    "description": "Weblate 实例地址",
    "type": "string",
    "hint": "例如 https://translate.example.com，不要以 / 结尾",
    "default": ""
  },
  "weblate_token": {
    "description": "Weblate API Token",
    "type": "string",
    "hint": "在 Weblate 用户 Profile → API Tokens 创建。以 wlu_ 或 wlp_ 开头",
    "default": ""
  },
  "default_project": {
    "description": "默认项目 slug",
    "type": "string",
    "hint": "所有操作的默认项目。可让 AI 列出项目获取 slug。注意区分大小写",
    "default": ""
  },
  "project_blacklist": {
    "description": "项目黑名单（每行一个 slug）",
    "type": "text",
    "hint": "被拉黑的项目不会出现在任何列表中。每行一个 slug，支持 # 开头的注释行。可让 AI 列出项目获取 slug",
    "default": ""
  },
  "enable_templates": {
    "description": "启用字符串模板功能",
    "type": "bool",
    "default": false
  },
  "templates_override": {
    "description": "手动编辑模板（JSON 格式）",
    "type": "text",
    "hint": "直接编辑模板 JSON。文件为空时自动导入，LLM 创建模板后自动同步显示。可通过聊天或手动编辑",
    "default": "",
    "editor_mode": true,
    "editor_language": "json"
  },
  "template_ai_provider": {
    "description": "创建模板时调用的 AI 模型",
    "type": "string",
    "hint": "用于将用户需求转为自然语言模板描述",
    "default": "",
    "_special": "select_provider"
  },
  "backup": {
    "description": "仓库备份",
    "type": "object",
    "hint": "定时备份 Weblate 仓库翻译文件，防止数据丢失",
    "items": {
      "enabled": {"type": "bool", "default": false, "description": "启用定时备份"},
      "project": {"type": "string", "default": "", "description": "要备份的项目 slug（空=所有项目，可让 AI 列出获取）"},
      "interval_hours": {"type": "int", "default": 1, "description": "小时级备份间隔"},
      "hourly_keep": {"type": "int", "default": 3, "description": "小时级备份保留数量"},
      "daily_time": {"type": "string", "default": "12:00", "description": "天级备份时间 HH:MM"},
      "daily_keep": {"type": "int", "default": 3, "description": "天级备份保留数量"}
    }
  },
  "default_lang": {
    "description": "默认语言代码",
    "type": "string",
    "hint": "创建翻译单元时的默认目标语言，如 en、zh_Hans",
    "default": "en"
  },
  "cache_ttl": {
    "description": "缓存过期时间（秒）",
    "type": "int",
    "hint": "查询结果缓存多久后重新拉取，0 表示不缓存",
    "default": 300
  }
}
